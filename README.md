# C URL Shortener

Just a command line url shorter.


build:

```
$cmake -H. -Bbuild

$cmake --build build

```

usage:

```
$.\bin\shorter

SET www.url.com

xjsikxzns

GET xjsikxzns

www.url.com

```

type `exit` to exit.
